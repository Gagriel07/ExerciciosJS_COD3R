const div = (dividendo, divisor) => {
    console.log(`O resultado de ${dividendo} / ${divisor} = ${dividendo / divisor}`)
    console.log(`E o resto da divisão é ${dividendo % divisor}`)
}

div(5, 2)